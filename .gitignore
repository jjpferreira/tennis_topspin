__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
.env

# Runtime logs from the Python app
python_app/logs/
*.log

# Local arduino-cli build artifacts (used by `arduino-cli compile
# --build-path .build_cache/firmware ...` for sandbox-friendly builds).
.build_cache/

# Claude Code worktree scratch space (local to this developer's machine,
# never source).
.claude/
